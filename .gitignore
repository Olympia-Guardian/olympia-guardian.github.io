node_modules/
dist/
*.local

.claude/settings.local.json
*.tsbuildinfo

# état local de wrangler dev (worker)
worker/.wrangler/
# secrets du worker en dev local : le vrai passe par wrangler secret put
worker/.dev.vars
worker/.dev-dump.sql

# genere au build depuis git log (scripts/changelog.mjs) : committe, chaque
# commit le rendrait obsolete
public/data/changelog.json

# Sauvegardes de la base : contiennent comptes, sessions et identifiants
# Discord. Ne doivent JAMAIS partir dans un depot, public ou non.
backups/
